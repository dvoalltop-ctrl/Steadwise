import { PlaceholderScreen } from '@/src/components/PlaceholderScreen';

export default function GardenRoute() {
  return (
    <PlaceholderScreen
      title="Garden"
      description="Beds, plantings, and harvest tracking are coming next. Your local database is already prepared."
    />
  );
}
