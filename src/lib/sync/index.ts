/**
 * Offline sync engine (placeholder).
 *
 * The change queue that reconciles local writes with the remote backend
 * will live here. Intentionally empty for now — the app runs on mock data.
 */
export {};
