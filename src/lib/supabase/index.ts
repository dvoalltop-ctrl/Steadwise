/**
 * Supabase client + remote sync (placeholder).
 *
 * Cloud auth and backend access will be configured here in a later phase.
 * Intentionally empty for now — the app runs entirely on mock data.
 */
export {};
