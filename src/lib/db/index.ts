/**
 * Local database layer (placeholder).
 *
 * Offline-first storage (e.g. SQLite via `expo-sqlite`) will live here.
 * Intentionally empty for now — the app runs entirely on mock data.
 */
export {};
