/**
 * @deprecated Import from `@/lib/db` instead.
 * Kept for backward compatibility with existing feature imports.
 */
export {
  initializeDatabase,
  getDatabase,
  resetDatabase,
  isDatabaseReady,
} from '@/lib/db/client';
